fun main (args: Array<String>) {

    var punchPower: Int = 90
    var punchSpeed: Int = 70

    if (punchPower > 80 && punchSpeed > 60){

        var overalPerformence : Int = punchPower * punchSpeed
        println(overalPerformence)
    }
}